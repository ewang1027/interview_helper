# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Node
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo

# Playwright: the minted session cookie, traces and failure screenshots. The state
# file holds a real signed session token — it is regenerated every run by the setup
# project, and it must never be committed.
apps/web/.playwright/

# Environment / secrets — never commit these
.env
.env.*
!.env.example
*.pem
*.key

# Terraform
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl
*.tfvars
!*.tfvars.example

# Local data
*.db
*.sqlite3
/data/
/tmp/

# OS
.DS_Store
Thumbs.db

# Executor scratch
apps/executor/scratch/

# Local database dumps — `make backup`. Personal data, and large.
/backups/

# pytest-cov output
.coverage
coverage.xml
htmlcov/

# Which Docker daemon this machine's data lives on (docs/INFRA.md#one-daemon-per-machine).
# Machine-local by nature — the other machine's answer is different.
/.docker-context
